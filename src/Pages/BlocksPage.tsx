import { Box, Typography } from "@mui/material";

export default function BlocksPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Gestion des Blocs</Typography>
      <Typography>Gérez vos fragments de contenu réutilisables ici.</Typography>
    </Box>
  );
}
